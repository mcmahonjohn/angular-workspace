# syntax=docker/dockerfile:1
FROM node:20 AS builder
WORKDIR /app
COPY . .
RUN npm ci --only=production && npm run build --if-present

FROM node:20-slim
WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY package*.json ./
RUN npm ci --only=production
EXPOSE 3000
CMD ["node", "dist/main.js"]
